{
  "name": "ui-consistency-investigator",
  "version": "0.2.1",
  "private": true,
  "description": "Configurable Chrome extension for deep UI consistency and design diagnostics.",
  "type": "module",
  "scripts": {
    "check": "node ./scripts/check.mjs",
    "build": "node ./scripts/build.mjs",
    "package": "node ./scripts/package.mjs",
    "test": "npm run check"
  },
  "engines": {
    "node": ">=20"
  }
}
